/**
 * Created by MLS on 15/3/10.
 */
define(function(){
   /*var port = window.location.port;
   if(port!=80){
       port = ":"+port+"/"
   }else{
       port = "/";
   }
   window.BASEPATH = window.location.protocol+"/"+window.location.hostname+port;*/

   window.BASEPATH = "http://localhost:8080";
});
